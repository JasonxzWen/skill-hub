interface:
  display_name: "Prototype"
  short_description: "Create throwaway logic or UI prototypes to answer one design question."
  brand_color: "#7C3AED"
  default_prompt: "Use $prototype to build a clearly marked throwaway prototype that answers this design question."
policy:
  allow_implicit_invocation: true
