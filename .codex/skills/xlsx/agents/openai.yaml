interface:
  display_name: "Xlsx"
  short_description: "Use this skill any time a spreadsheet file is the primary input or output. This means any task where the user wants to: open, read, edit, or fix an existing..."
  default_prompt: "Use $xlsx to help with this task."

policy:
  allow_implicit_invocation: true
