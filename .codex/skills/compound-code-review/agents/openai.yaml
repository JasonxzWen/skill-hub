interface:
  display_name: "Compound Code Review"
  short_description: "Run structured CE-style code review with reviewer lenses, confidence anchors, and fix routing."
  brand_color: "#7C3AED"
  default_prompt: "Use $compound-code-review to review current changes with structured findings, reviewer lenses, anchored confidence, and action routing."
policy:
  allow_implicit_invocation: true
