interface:
  display_name: "Diagnose"
  short_description: "Debug runtime bugs with feedback loops, hypotheses, probes, and regression tests."
  brand_color: "#DC2626"
  default_prompt: "Use $diagnose to reproduce this failure, rank hypotheses, instrument narrowly, fix it, and add a regression test."
policy:
  allow_implicit_invocation: true
